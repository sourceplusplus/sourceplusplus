mutation addAccessPermission($locationPatterns: [String!], $type: AccessType!) {
    addAccessPermission(locationPatterns: $locationPatterns, type: $type) {
        id
        locationPatterns
        type
    }
}